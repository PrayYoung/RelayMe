# Copy to a secure local file outside this repository, then replace every value.
RELAYME_URL=https://controller.example.invalid:8765
RELAYME_TOKEN=CHANGE_ME_SCOPED_R0_TOKEN
# Optional when the Controller uses a private CA.
RELAYME_CA_CERT=/secure/local/path/controller-ca.pem
